fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt >= 4.1
python-multipart==0.0.18
jinja2==3.1.4
python-dotenv==1.0.1
pydantic-settings==2.6.1
tzdata==2024.2  # timezone database for zoneinfo on Windows (DISPLAY_TIMEZONE)

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.27.2
anyio==4.7.0

# Slice 4 — queue
rq==2.0.0
redis==5.2.1

# Slice 5 — XRPL
xrpl-py==4.0.0
cryptography==43.0.3

# Slice 8 — perf testing
locust==2.32.4
